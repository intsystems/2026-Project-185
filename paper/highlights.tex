\begin{highlights}
\item TEMPO discovers latent solution regimes from unlabelled PDE snapshots via EM.
\item Regime-local bases provably achieve no greater error than a global POD basis.
\item TEMPO(POD) reduces error by 13--50\% on Burgers' and up to $5\times$ on Darcy flow.
\item Per-parameter specialists fail catastrophically out-of-distribution.
\item Regime membership reflects initial-condition geometry, not the physical parameter.
\end{highlights}
